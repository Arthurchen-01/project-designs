# TASK-T001 Review — 模型调用路径修复 + 密钥安全加固

**Reviewer:** Agent 3
**Date:** 2026-04-02 11:15 CST
**Verdict:** ⚠️ CONDITIONAL PASS — AI response quality needs follow-up

---

## Review Findings

### 1. Root Cause: systemd pointing to old code path ✅ Correctly Identified

The systemd service pointed to `/home/ubuntu/ap-tracker` (old Agent 2 deployment) instead of `/root/.openclaw/workspace-agent3/30_execution/ap-tracker`. The old `.env` had `AI_BASE_URL=http://localhost:8000/v1` — no local model running there, causing all AI calls to ECONNREFUSED and fallback to rule engine.

**Fix verified:** New service file points to correct path with correct env vars.

### 2. systemd Service Rebuild ✅ PASS

- Service rebuilt with correct working directory and AI_* environment variables
- `daemon-reload` + restart confirmed
- Process environment verified to contain correct `AI_BASE_URL` / `AI_API_KEY`

### 3. ai-evaluator.ts Enhancement ✅ ACCEPTABLE

- Reasoning model content extraction via `reasoning_details` — correct approach for MIMO on OpenRouter
- Robust JSON extraction (markdown fence, brace matching) — good defensive coding
- `max_tokens: 500` — acknowledged as likely insufficient (see blocker below)

### 4. Key Security ✅ PASS

| Check | Result |
|-------|--------|
| `.env` in `.gitignore` | ✅ Confirmed |
| `.next/` bundle contains no `sk-or` | ✅ 0 matches |
| Key only in systemd service (root readable) | ✅ Confirmed |
| No hardcoded keys in `src/` | ✅ Confirmed |

### 5. Integration ✅ PASS

- Nginx → localhost:3000 proxying works
- Domain `samuraiguan.cloud` resolves correctly
- Frontend no longer exposes API keys

---

## Remaining Issue: AI Response Quality ⚠️

**Problem:** mimo-v2-pro is a reasoning model. It spends most of its `max_tokens` budget on `reasoning_details` before producing actual output. At `max_tokens: 500`, the response is likely truncated before valid evaluation JSON can be extracted.

**Impact:** AI evaluation endpoint technically works (no ECONNREFUSED), but response quality is poor — may return incomplete JSON or echo rather than structured assessment.

**Recommended fix for follow-up task:**
- Option A: Increase `max_tokens` to `3000` (or higher)
- Option B: Switch to a non-reasoning model variant if available (e.g., `xiaomi/mimo-v2-fast` or similar)
- Option C: Add a system prompt tweak to minimize reasoning output for structured tasks

---

## Recommendation for Agent 1

1. **Dispatch a follow-up P1 task:** Fix MIMO `max_tokens` or model choice to get reliable evaluation JSON responses
2. **Accept TASK-T001 as conditionally passed** — the pipeline is correct, systemd is fixed, security is sound. The remaining issue is tuning, not architecture.
3. **Proceed with FD-011 (delivery prep)** and **FD-012 (regression)** — but note that AI evaluation quality will affect the regression suite if model calls are tested end-to-end.
4. **Update STATUS.md** — current status is stale.
