# TASK-030 Review — 2026-03-31

**Reviewer:** Agent 3
**Target:** daily-update P1 field fix + confidence P2 display fix
**Branch:** nightly/2026-03-31-confidence-fix

---

## Verdict: ❌ REWORK REQUIRED

## P1 Fix — REGRESSION INTRODUCED

Agent 2 changed the **frontend** to send `activityType`, `durationMinutes`, `description` — but the **API route** (`/api/daily-update/route.ts`) still destructures `taskType`, `timeMinutes`, `notes` from the request body.

```typescript
// API still expects:
const { taskType, timeMinutes, notes } = body

// Frontend now sends:
{ activityType: form.taskType, durationMinutes: ..., description: ... }
```

Result: `taskType` → `undefined`, validation fails (`!taskType`), submit blocked.

**Required fix:** Either:
- **Option A (preferred):** Change API destructuring to accept new field names (`activityType`, `durationMinutes`, `description`) and update the internal references
- **Option B:** Add a compat mapping layer in the API that accepts both old and new names

## P2 Fix — ✅ PASS (with caveat)

`personal/page.tsx` and `personal/[subjectId]/page.tsx` both now read `data.confidenceLevel` with client-side fallback. Logic is correct.

**Caveat:** The fallback (`avgFiveRate >= 75`) means if the API doesn't return `confidenceLevel`, users still see static confidence. This is acceptable for now but should be monitored.

## Action Items

1. **Agent 2 must fix the API destructuring** to match the new frontend field names (or revert frontend and change API to use the old names)
2. Verify daily-update submission actually works end-to-end after fix
3. No change needed for P2 — confidence display is correct

## Files Changed (for reference)

- `30_execution/ap-tracker/src/app/[classId]/daily-update/page.tsx` — frontend field rename
- `30_execution/ap-tracker/src/app/[classId]/personal/page.tsx` — confidenceLevel usage
- `30_execution/ap-tracker/src/app/[classId]/personal/[subjectId]/page.tsx` — confidenceLevel usage
