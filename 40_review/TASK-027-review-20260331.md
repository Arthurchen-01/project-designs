# Review Report — TASK-027 Batch 2 (Nightly fixes per baseline review)

**Reviewer:** Agent 3
**Date:** 2026-03-31T04:28+08:00
**Target:** TASK-027 — Confidence recency + mock-data cleanup + TS fixes
**Branch:** main (commit `be37596`)

---

## Verdict: ✅ PASS

---

## Checklist Audit

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | mock-data.ts 删除 | ✅ | `ls` confirms file gone; daily-update page no longer imports it |
| 2 | confidence.ts recency 感知 | ✅ | `getConfidenceLevel(recordCount, daysSinceLastRecord)` — high: ≥5且≤30天, medium: ≥2且≤60天, low: 其他 |
| 3 | P1 硬编码日期修复 | ✅ | scoring-engine.ts:70 `new Date()`, :86 `new Date()` — 已修复 |
| 4 | ai-config.ts `getAIConfig()` + `isAIEnabled()` | ✅ | 函数导出，环境变量读取，无硬编码 key |
| 5 | ai-advisor.ts 使用 getAIConfig() | ✅ | 不再引用 `AI_CONFIG` 常量 |
| 6 | ai-explainer.ts 使用 getAIConfig() | ✅ | 同上 |
| 7 | calculateConfidence() 兼容导出 | ✅ | `calculateConfidence(testCount, _totalCount, daysSinceLastRecord)` 委托给 `getConfidenceLevel` |
| 8 | Build 通过 | ✅ | 22 routes, compiled successfully |

---

## Code Review: confidence.ts

```typescript
export function getConfidenceLevel(
  recordCount: number,
  daysSinceLastRecord?: number
): ConfidenceLevel {
  const daysSince = daysSinceLastRecord ?? 999
  if (recordCount >= 5 && daysSince <= 30) return 'high'
  if (recordCount >= 2 && daysSince <= 60) return 'medium'
  if (recordCount >= 5) return 'medium'  // enough records but stale
  return 'low'
}
```

**Positive:** 逻辑清晰，stale fallback（≥5条但超30天 → medium 而非 low）合理。
**Minor:** `daysSinceLastRecord` 为可选参数，未传时默认 999（→ low）。调用方需确认始终传入该值，否则所有学生都会是 low confidence。

---

## Verification

| Test | Result |
|------|--------|
| `src/lib/mock-data.ts` 不存在 | ✅ |
| scoring-engine.ts 无硬编码日期 | ✅ |
| `src/lib/ai-config.ts` 存在且导出 `getAIConfig` | ✅ |
| `src/lib/ai-advisor.ts` 引用 `getAIConfig` | ✅ |
| `src/lib/ai-explainer.ts` 引用 `getAIConfig` | ✅ |
| `confidenceLabel()` + `confidenceHint()` 存在 | ✅ |

---

## Decision for Agent 1

**TASK-027 Batch 2 可以关闭。** 核心修复（P1日期、recency confidence、dead code清理）全部到位。

Phase 4 + Nightly Batch 1 状态：
- ✅ TASK-023~026 AI 集成（已通过审查）
- ✅ TASK-027 基线修复（本次审查通过）
- ✅ TASK-028 产品基线评估（已通过）

建议 Agent 1 规划下一批次：N+1 advice API 优化、error boundary、dashboard alerts 前端接入。
