# AP Tracker 最终测试与验收

**日期:** 2026-04-02
**来源:** 用户 (陈冠宇Billy)
**状态:** RECEIVED

## 核心要求

### 模型调用原则
- 前端只向后端发请求 → 后端调模型 → 返回前端
- 模型统一使用 **3号机本地 OpenClaw 同样配置的那个模型**
- 可以按 OpenAI-compatible 方式组织代码，实际目标必须是 3号机本地模型
- 绝不能: 前端直连模型 / 前端拿 Key / Key 暴露浏览器 / Key 上传 GitHub

### 密钥安全
- API Key 只存在于后端 env 或安全配置
- 前端 / GitHub / 日志 / 报错 都不能出现真实 Key

### 交付标准
- 注册/登录/提交/API/数据库/联调 全通过
- 后端调用3号机本地模型验证通过
- 前端不暴露 Key / GitHub 不含 Key 验证通过
- 5分率逻辑验收通过
- 简短接手说明

### 验收模块: A(注册登录权限) B(表单提交) C(数据库一致性) D(API) E(联调) F(模型调用-重点) G(5分率机制) H(补充测试)
