# TASK-032 Test Plan

## Verification goals
1. The report resolves the `dispatch/` vs `20_tasks/` conflict explicitly
2. The report resolves the `ROLE.md` vs `AGENTS.md` source-of-truth conflict explicitly
3. The path-mapping table is specific enough for a later scaffold task
4. The result reduces overlap instead of generating new duplicate templates

## Pass criteria
- A reviewer can state where each control-plane concept lives today vs in the future repo
- A reviewer can explain which file is primary for role definition
- The result is specific enough to unblock a repo-ready starter pack task
