# Execution Checklist for TASK-DEP-001

1. SSH into server #3.
2. Run commands for system info (uname, lsb_release, etc.).
3. Check CPU/memory/disk (top, df, free).
4. List occupied ports (netstat/ss).
5. List running services (systemctl/ps).
6. Check firewall status (ufw/iptables).
7. Check timezone (timedatectl).
8. Assess suitability and note issues.

