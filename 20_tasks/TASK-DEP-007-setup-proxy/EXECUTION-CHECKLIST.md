# Execution Checklist for TASK-DEP-007: Set Up Reverse Proxy

1. Install local proxy software (e.g., Nginx from local package).
2. Configure proxy rules for backend and admin.
3. Start proxy service.

Enforce: Local config, no external dependencies.