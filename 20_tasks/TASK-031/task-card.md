# TASK-031 — Control-plane repository skeleton and file map

## What to do
Create a minimal, explicit control-plane repository skeleton proposal for the new multi-agent productization direction.

The output should define the baseline structure only. Do **not** implement runtime orchestration yet.

Required scope:
- proposed top-level directories
- purpose of each directory
- which files are mandatory in M1
- clear distinction between control repo and target business repos

Expected deliverable location for execution output:
- `30_execution/TASK-031-report.md`

## Why this task exists
The user provided a new productization input package and explicitly asked not to let execution freestyle. We need the smallest concrete first step that turns the product direction into a buildable structure.

## Constraints
- Keep it small
- No broad speculative runtime implementation
- No replacing current system state
- Focus on M1 skeleton only

## Report back
Agent 2 should report:
1. what structure was proposed
2. why it is minimal but sufficient
3. what open questions remain
