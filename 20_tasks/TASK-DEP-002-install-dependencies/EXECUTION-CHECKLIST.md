# Execution Checklist for TASK-DEP-002: Install Local Software Dependencies

1. Locate local package repositories or bundles on Machine 3.
2. Install Node.js from local source.
3. Install database software (e.g., PostgreSQL) locally.
4. Install any other required libs (e.g., npm packages from local tarballs).
5. Verify installations.

Enforce: Use only local files, no npm install from internet.